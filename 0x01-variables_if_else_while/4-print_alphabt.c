#include <stdio.h>
/**
 * main - main block
 * description: get a random number and check its
 * return: 0
 */
int main(void)
{
	char c = 'a';
	while (c <= 'z')
	{
		if (c != 'e' && c != 'q'i)
		{
		
	         	putchar(C);
		
		}c++;
	}
	putchar('\n');
	return (0);
}

